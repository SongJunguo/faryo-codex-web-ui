# Faryo v1.10 Release and Maintainability Plan

状态：已完成（2026-08-22）

开发分支：`feature/v1.10-maintainability`

基线：Faryo `1.9.1`，`main` 提交
`17cf3b9cbef309f9ad9ddbf7702cd23833e82280`

开始日期：2026-08-22

## 1. 背景与结论

Faryo 1.9 已经完成 Codex App Server 正文流、Owner ASGI、单写者会话、断线恢复、
Preact 交互壳和 tmux 兼容路径。当前运行健康，下一阶段不需要再做一次框架重写。

现在最值得处理的三个问题是：

1. `main` 已是 1.9.1，但最新公开 tag/Release 仍停在 1.6.7；新用户按 README
   下载会得到旧架构，发布入口与源码事实不一致。
2. 维护成本仍集中在少数热点：Owner `server.py` 约 3,700 行、Owner `app.js`
   约 2,600 行，Gateway `workbench.js` 约 1,800 行。已有服务模块和 Preact 岛，
   但 composition root 仍承担过多转换、状态拼装和 DOM 协调。
3. 项目精确固定 Python、Node 和 GitHub Actions 依赖，但缺少自动依赖更新入口、
   静态安全扫描和显式 Ubuntu 22.04 兼容门。Faryo 是远程控制面，这些属于值得
   自动化的维护工作。

本阶段采用渐进拆分和可验证的供应链加固，不增加原生 App、Web Push、多人协作、
第二套后端或不安全的 TUI/Web 自动接管。

## 2. 用户可感知结果

- GitHub 的 Latest Release 与 `main`/README 不再相差多个版本；安装器默认获得当前
  App Server 流式架构。
- 更新依赖或 Codex 后，CI 能更早发现 Python、Node、浏览器 bundle、协议和 Ubuntu
  22.04 兼容回归。
- 后续修复发送、历史、流式输出或交互菜单时，修改范围更小，不必继续扩大两个前端
  composition root 或 Owner 主服务文件。
- 新建对话只打开一个现代化选项窗口：即使配置多个工作站，也在同一页选择工作站、
  `Codex App Server` / `Codex TUI (tmux)`、工作目录和上下文窗口。恢复非活动 Codex 历史
  对话复用同一套后台、目录和上下文控件。
- 对话页 Details 明确显示当前使用 `Codex App Server` 还是 `Codex TUI (tmux)`，不要求
  用户根据 tmux 名称、source 字段或实现术语猜测。
- `/model`、`/usage`、`/goal clear` 等 Codex 本地命令不再共用一个粗糙的“必须
  Waiting”门禁；Faryo 根据当前 Codex 版本和命令能力决定能否在任务运行时执行。
- `Goal Active` 状态可点击查看当前目标、状态和可安全公开的进度字段；清空 Goal 后不再
  残留 `done` 或上一个会话的内容。
- New/Resume 选项继续提供完整目录浏览、隐藏目录开关与自定义 context window；页面中的
  cwd、Git 和运行状态来自当前会话的权威快照，不因旧缓存或异步响应回退。
- 部署、普通刷新、移动键盘、Markdown/TeX、长历史、认证和 tmux 几何保持现状。

## 3. 非目标

- 不把 Python 后端改写成 Go、Rust 或 Node。
- 不把整个前端一次性改写成 React/Preact，也不虚拟化已经通过验收的 rich transcript。
- 不删除 terminal-managed/tmux 兼容模式。
- 不实现未经官方协议证明安全的 Web/TUI 双向 handoff。
- 不把当前 Codex CLI 的 46 条命令当作永久固定清单，也不通过解析终端像素位置猜测菜单。
- 不实现可编辑 pending queue、后台 Web Push、语音、公共分享或原生移动 App。
- 不更改 Cloudflare Access、Gateway Cookie 或 Owner token 的操作者配置。
- 不记录 prompt、answer、会话正文、私有路径、账号、域名、Token 或 Cookie。

## 4. 不可回归约束

1. **单写者**：Web-managed thread 只能由持久 App Server actor 写入；terminal-managed
   thread 只能由现有 TUI 路径写入。
2. **正文真相**：Codex rollout/App Server history 仍是持久正文来源；Faryo 不建立正文数据库。
3. **认证**：Browser → identity edge → Gateway → Owner → Unix socket 的边界不变；Owner、
   Gateway 继续只监听 loopback，App Server 不增加 TCP 监听。
4. **可靠发送**：submission ID、快速 ACK、重试幂等、失败草稿和 queued Send now 不回归。
5. **渲染**：Markdown AST、KaTeX、Shiki、Raw/Chat 隔离、复制原始 Markdown/TeX 不回归。
6. **长历史与移动端**：有界 rich DOM、前插锚点、问题导航、原生 viewport-resize 和透明
   composer 不回归。
7. **普通刷新**：可变资产继续自动版本化；新 tab 或普通 reload 即可获得新代码。
8. **tmux**：升级、服务重启和测试不得修改既有 pane PID、名称、宽高或 TUI wrapping。
9. **公开隐私**：计划、fixture、CI 日志和 Release 不出现真实会话、私有路径或凭据。
10. **会话身份**：App Server 与 TUI 共用唯一的 `faryo数字` 名称空间；启动响应必须回显
    本次 client launch ID，浏览器不得接受旧请求或另一会话的跳转目标。

## 5. 工作流 A：补齐 1.9.1 稳定发布

1. 以当前已验证的 `main` 提交创建不可变 `v1.9.1` tag，不让后续 v1.10 改动混入。
2. 复核版本四处一致、Release notes、source archive、checksum 和 bootstrap installer。
3. 运行 canonical source gate、公开差异隐私扫描和 GitHub Release workflow。
4. 在全新临时目录通过 Release 资产安装/doctor/update/rollback 合同；不触碰现有私有数据。
5. 验证 GitHub `releases/latest` 指向 1.9.1，README 安装示例不再指向 1.6.7。

## 6. 工作流 B：Owner 后端热点拆分

先用函数调用图和现有测试确定边界，再移动代码。目标不是追求小文件，而是让路由、
状态投影和领域服务只有一个所有者。

优先候选边界：

- session/history catalog：Codex thread 列表、标题、cwd、archive/search/page 投影；
- status/workbench projection：tmux、App Server、goal、usage、Git 和 ownership 的只读合并；
- launch/resume policy：目录 token、context window、运行时 ownership 和幂等 launch receipt；
- composition wiring：`server.py` 只装配配置、服务对象和兼容导出，不重新实现领域规则。

实施规则：

- 新模块先通过现有函数的契约测试，再切调用点；
- 不保留两份同功能实现或永久 forwarding wrapper；
- 不把 ASGI Request/Response 对象传入领域模块；
- 可测试纯逻辑使用标准库 dataclass/TypedDict，不为搬文件引入新运行时框架；
- terminal-managed helper 必须在命名和类型上保持只读/兼容边界，不能成为第二个 writer。

量化目标：`server.py` 至少净减少 20%，且迁出的每个模块有聚焦单元测试；如果调用图
证明某一候选边界会增加循环依赖，则记录证据并选择更窄边界，不用行数目标强迫错误拆分。

## 7. 工作流 C：Owner/Gateway 前端 composition root 收敛

保留当前 Preact 岛和高性能 imperative transcript，不做全量 framework migration。

1. 盘点 `app.js` 与 `workbench.js` 的网络、状态、渲染和事件责任。
2. 优先把 App Server stream/snapshot/revision 投影、session ownership 和可复用 API 数据
   normalization 移入严格 TypeScript 或可独立 Node 测试的 ES module。
3. `app.js` 只保留 rich Markdown/TeX、Raw、Live tmux、历史岛之间的 composition adapter；
   不让 Preact 再实现第二套正文 renderer。
4. Gateway 继续让 Preact keyed lists 拥有动态卡片；`workbench.js` 不重复拼接这些节点。
5. 每个新模块需有 stale generation、out-of-order event、ordinary reload 和 inert-text 测试。

### 会话后台选择与可见性

- 面向用户只显示 `Codex App Server` 和 `Codex TUI (tmux)`；内部代码使用集中定义的
  `APP_SERVER` / `CODEX_TUI` 类型或常量。为避免迁移私有 registry 和破坏 Gateway/Owner
  滚动升级，wire/storage 值继续兼容稳定枚举 `web-managed` / `terminal-managed`，但不得
  原样泄露到按钮、错误提示或 Details 文案。
- 新建会话的 Options 窗口同时显示工作站、后台、目录和上下文选择，默认
  `Codex App Server`；单工作站不再先弹冗余确认，多工作站也不得退回两级 sheet。
- 非活动、未归档、可恢复的 Codex thread 在 Options 窗口中显示后台选择器；默认选中该
  thread 最近一次/来源对应的后台，操作者可以显式改选。
- 普通卡片点击仍使用最近后台快速恢复；`Options` 才负责显式后台、cwd 和 context window。
- 切换后台只发生在 resume 之前。若 thread 已被任一 App Server actor 或 tmux/TUI writer
  占有，Owner 必须原子地返回 409，不能先启动第二个 writer 再清理第一个。
- App Server 来源的历史允许显式恢复到 TUI，TUI 来源的历史也允许显式恢复到 App Server；
  两条路径都复用同一 cwd/root/token/context 校验，不根据旧 source 绕过安全策略。
- Details 从当前 status 的权威 backend 字段映射人类可读名称；terminal status 也必须显式
  返回 backend，不能依赖缺字段回退猜测。
- `web-managed` / `terminal-managed` 限制在协议适配、旧 registry 读取和兼容测试中；新增
  领域代码、变量和类型统一使用 `APP_SERVER` / `CODEX_TUI`。若将来升级协议值，必须先做
  显式 schema/version 迁移，不能在这轮静默改写现有私有状态。
- New 与 Resume 共用一个后台选择模型和校验器，不分别维护两套字符串、默认值或标签映射。
- TUI 分配名称时必须保留 App Server registry 中的名称，App Server 分配名称时继续保留
  tmux 名称，并由同一进程锁消除并发竞态。升级发现历史冲突时原子迁移持久 App Server
  映射，不关闭 TUI、不中断 Codex thread。
- 每次 New 使用新的 client launch ID；网络重试只能复用同一次 ID。Gateway 响应回显该 ID，
  前端同时核对 ID、session 格式和 redirect 三者，拒绝缓存或迟到的旧启动响应。

### 交互命令、Goal 与运行状态

- 命令目录以运行中的 Codex 能力探测为主、与已验证版本绑定的快照为回退；新增、删除或
  改变忙碌期语义时形成可诊断 drift，不把代码中的 46 项清单当永久事实。
- 命令能力至少区分 App Server RPC、TUI 本地命令、普通 prompt、交互式菜单以及
  `available_during_task`。App Server 与 TUI 使用各自的官方入口，不把控制命令误排成消息。
- 当安装的 Codex 版本与已验证快照不一致时，未知的忙碌期能力 fail closed；普通可用命令
  仍可显示，诊断中只记录版本和命令名，不记录正文。
- `/goal clear` 等经当前 Codex 官方源码确认可在任务中执行的 TUI 命令立即转发；确实被
  Codex 禁用的命令给出明确原因，不返回笼统的“Codex is not ready”。
- `/model`、恢复目录选择等交互式状态必须由结构化 interaction state 驱动；菜单打开、选择、
  取消、断线恢复和旧响应隔离有测试，不依赖固定等待秒数或盲发 Enter。
- Goal 状态使用权威 goal snapshot；状态徽标可打开详情，展示 objective、active/done/blocked
  与已有安全进度字段。无 Goal 时隐藏或明确显示为空，不复用上一会话状态。
- cwd、Git、model、usage、backend 和 lifecycle 共享带 generation/revision 的状态投影；
  session 切换、普通 reload 与延迟响应不能把旧值写回当前页面。
- 目录浏览默认不隐藏普通子目录；隐藏目录由显式开关控制。New/Resume 均保留最近目录、
  上一级导航、手工选择目录和以 k 为单位的 context window，并在服务端重新校验路径。

### App Server 正文、流式尾部与问题导航

- App Server 已提供带 item/turn identity 的结构化 user/assistant/process 数据；前端不得再把
  整段 transcript 交给 terminal compact parser。角色块必须由结构化 item 直接生成并保留
  稳定 key，终端解析器只用于 TUI/旧兼容数据。
- 进行中的 App Server capture 必须与已加载历史按 item identity 合并，不能让 settled history
  覆盖 live capture。用户消息立即出现，assistant delta 至少形成两个可观察长度阶段，完成后
  原位收敛到最终 rollout。
- 首个 assistant delta 前显示明确但非阻塞的 `Codex is working…`，正文到达后显示
  `Receiving response…`；完成、取消、断线和切换会话时必须消失。
- App Server history page 同时返回匿名结构化 blocks 与原文本兼容字段；问题 marker 的 key
  直接关联相应 user block。已加载 marker 必须可跳转，不能出现“有 14 个 marker 但 0 个
  user anchor”的假导航。
- 历史分页优先按 actor 已脱敏的 `messageBlocks.turnKey` 分组，因为真实
  `turn/completed.items` 可能只有 assistant item；不能假定 raw turn 内含用户消息。旧协议没有
  `messageBlocks` 时才使用 raw turn 兼容投影。
- 活动 App Server 会话从创建、流式、Owner 重连到关闭必须始终使用同一套 App Server
  turn/item identity；不得因 rollout JSONL 刚刚出现就中途切换问题 key。会话关闭后，持久
  JSONL 仍是恢复历史的权威来源。
- Owner 重连后 `thread/read` 可能不再返回已完成的 command items；历史层必须只从同一
  thread 的 rollout 恢复有界 command/search/edit activity，以稳定 turn/call ID 合并去重，
  不能切换问题 key、展示 reasoning/tool output，或要求硬刷新。
- 真实长会话验收同时统计 API roles、DOM user/output blocks、流式长度阶段和 loaded marker，
  并实际点击一个已加载 marker，验证它落到对应 user block；不再以“看到 streaming=true、
  看见若干刻度或最终正文”冒充真实流式 UI/问题导航已经通过。
- 对不存在或已关闭会话建立 SSE 时，Owner 必须在响应头发送前返回有界 JSON 404；已打开的
  App Server stream 在会话消失时安静结束，不能产生 `response already started` 异常风暴。

量化门：至少移除一个完整的状态责任，而不是把同样闭包机械切成多个文件；生成 bundle
必须可复现，gzip 预算、许可证文件和 CSP 不变或有明确记录。

## 8. 工作流 D：依赖与供应链自动化

1. 增加 Dependabot（或经审计后更合适的等价工具）处理 npm、pip 与 GitHub Actions，
   使用低噪声的每周分组 PR，不自动合并。
2. 对第三方 GitHub Actions 固定完整 commit SHA，并保留可读版本注释；更新由 bot 提 PR。
3. 增加官方 CodeQL Python/JavaScript-TypeScript 扫描；无权限或平台限制必须显式报告，
   不能假装通过。
4. CI 增加 Ubuntu 22.04/Python 3.10 的真实最低支持 lane，并保留当前 Python 3.13 lane；
   避免把完整浏览器矩阵重复到每个组合导致 CI 失控。
5. 继续执行 `npm audit`、锁文件一致性、bundle byte-compare 和依赖许可证账本检查。
6. 自动化只处理公开依赖元数据，不读取部署配置、会话或私有文件。

## 9. 工作流 E：版本化浏览器 API 契约

1. 列出 Owner → browser 的 status、snapshot、event、interaction、history 和 error envelope。
2. 为跨 Python/TypeScript 的核心 envelope 建立一个版本字段和匿名 fixture；优先使用
   TypedDict/TypeScript 类型与现有契约测试，不默认引入 Pydantic。
3. 未知向后兼容字段可忽略；未知写 action、server request 或 ownership 值 fail closed。
4. Codex 升级后的 capability probe 必须区分 optional drift 与 required-method breakage。
5. 浏览器旧连接通过 generation/epoch/revision 被隔离，不能把旧响应写入新 session。
6. Terminal-managed slash command 按当前 Codex 的 `available_during_task` 能力分类；例如
   `/goal clear` 可在任务运行时由 TUI 本地处理，不能再被笼统的 ready 门禁拒绝。未验证
   命令继续保守拒绝，避免把控制命令误排成普通 follow-up。
7. command catalog 记录 `observedCodexVersion`、`testedCodexVersion` 和能力来源；版本不匹配
   时不能从旧快照静默继承 `available_during_task=true`。
8. status/goal/interaction envelope 都带可判定新旧的 generation/revision；前端只接受当前
   session 的响应，消除固定五秒等待和必须强制刷新的恢复路径。

## 10. 实施阶段

### Phase 0：基线与发布审计

- [x] 确认 Git、CI、服务、App Server socket 和 tmux geometry 基线。
- [x] 固定当前 tracked bytes、热点行数、测试数量和 bundle 预算。
- [x] 审计 GitHub tag/Release、安装示例与版本元数据。

### Phase 1：1.9.1 source-only Release

- [x] 完成 1.9.1 tag、Release workflow、checksum 和隔离 Release 资产验证。
- [x] 更新 README 的 Latest Release、安装示例和默认 Web-managed 启动说明。

### Phase 2：后端责任拆分

- [x] 形成 `server.py` 函数/调用/状态所有权清单。
- [x] 提取 session catalog 与 terminal launch/lifecycle 两个可独立测试的领域边界。
- [x] 删除迁移后不可达的重复实现，保持 ASGI 路由合同。

### Phase 3：前端状态边界

- [x] 形成 `app.js`/`workbench.js` 状态所有权清单。
- [x] 将 status 请求、取消、超时、并发合并和 session-generation 校验作为一个完整责任提取到
  `status-controller.mjs`，并用独立 Node 测试覆盖迟到响应与切换会话。
- [x] 保持 Preact keyed ownership、rich transcript 和长历史性能门；完整 source gate 与可复现
  bundle 通过。
- [x] 新建与恢复 Options 都增加 `Codex App Server` / `Codex TUI (tmux)` 选择并贯穿
  Gateway/Owner；新建默认 App Server，恢复默认最近后台。
- [x] Details 已从权威 status backend 显示明确名称；terminal/web 与普通 reload 的真实浏览器
  复验留在 Phase 6。
- [x] 集中后台类型、协议适配和用户标签；新增领域代码中不再传播抽象 wire 字符串。
- [x] Goal 徽标可展开权威详情，clear/session switch/reload 由 generation fencing 隔离旧状态。
- [x] New/Resume 的目录、隐藏项开关、context window 与后台选择可组合使用，路径仍由 Owner
  重新校验；New 的工作站选择也已合并到同一页，不再出现两级选择。
- [x] App Server/TUI 使用统一 session 名称空间；历史冲突可恢复迁移，New 跳转受本次
  client launch ID 约束，不会因同名后台或旧响应打开旧对话。

### Phase 4：供应链与兼容 CI

- [x] 配置 npm、pip 与 GitHub Actions 的每周分组依赖更新，不自动合并。
- [x] 固定 Actions 完整 SHA 并增加 Python/JavaScript CodeQL。
- [x] 增加 Ubuntu 22.04/Python 3.10 最低 lane，并保留 Ubuntu 24.04/Python 3.13；
  本地锁文件、许可证与 bundle 门通过，等待推送后的 GitHub runner 复验。

### Phase 5：浏览器 API 契约

- [x] 建立 `envelopeVersion: 1` 匿名 fixture 与 Python/JavaScript 双侧测试；缺字段兼容旧端，
  显式未知版本 fail closed。
- [x] 覆盖 unknown field/action、stale generation、epoch reset 和 protocol drift。
- [x] 命令目录按 Codex 版本合并 runtime probe 与回退快照；版本漂移测试证明未知忙碌能力
  不会被错误放行。
- [x] 覆盖 TUI 忙碌期 `/goal clear`、被禁用命令、App Server RPC、`/model` 菜单和恢复目录
  interaction，不使用固定睡眠作为正确性条件。
- [x] status/Details/Goal/Git/cwd 共用 session scope/generation；聚焦测试证明切换或取消后的
  迟到 status 不会覆盖当前页面，真实 reload/reconnect 复验留在 Phase 6。

### Phase 6：集成、部署与发布

- [x] canonical source check、双 Ubuntu/Python CI、双语言 CodeQL 和隐私扫描通过。
- [x] 隔离真实 Codex App Server 的 delta/final/approval/input/restart 通过。
- [x] Gateway/Owner 登录、普通 reload、手机布局、长历史与公式渲染通过；真实 App Server
  角色/流式/问题锚点回归修复后重验通过。
- [x] 可靠发送、SSE replay、archive/search、目录选择和 Goal/usage 通过；App Server live
  capture 与 settled history 按稳定 item identity 合并并通过真实浏览器验收。
- [x] 既有六个 tmux pane PID 与宽度全部保持；一个被真实窄终端客户端占用的 pane 高度随
  attached client 从 47 变为 25，Owner 以 `pane_width=0` 运行且未执行 resize，未人为改回。
- [x] 更新 README、架构/交互说明、依赖账本、计划证据和 Release notes。
- [x] feature 分支和 PR 的最终 Source CI/CodeQL 全绿，PR 以同一提交 fast-forward 合并
  `main`。
- [x] 发布 source-only `v1.10.0`；Release 资产的双 SHA-256、归档 CLI 版本和安装脚本帮助
  入口均从 GitHub 下载后复验通过。

## 11. 验收矩阵

| 场景 | 通过条件 |
| --- | --- |
| 公开发布 | `releases/latest`、README、版本元数据、tag 和 checksum 一致 |
| 后端拆分 | 路由不变、无双实现、无 ASGI 类型泄漏到领域层、热点净下降 |
| 前端拆分 | stale/out-of-order 事件隔离，rich transcript 不重复，不增加另一 renderer |
| 后台选择 | New/非活动 Resume 均可显式选 App Server/TUI；活动 writer 竞态 fail closed、无双写 |
| 新建流程 | 单/多工作站均只有一个 Options 页；切工作站异步更新目录，启动只跳本次新 session |
| 会话命名 | App Server/TUI 无同名 session；旧冲突迁移不关闭进程、不丢 thread、无歧义路由 |
| Details | 只显示 `Codex App Server` 或 `Codex TUI (tmux)`，刷新和切换会话后准确 |
| 内部命名 | 领域代码使用 `APP_SERVER`/`CODEX_TUI`；旧 wire 值只存在于集中兼容层和测试 |
| 命令漂移 | 当前版本能力生效；版本不匹配不继承旧 busy-safe 权限；目录增删可诊断 |
| TUI 命令 | 运行中 `/goal clear` 立即进入 TUI；真正不可用的命令给出准确提示且不变成 prompt |
| 结构化菜单 | `/model` 与 resume cwd 可选择、取消、重连；无盲发 Enter、无固定等待门槛 |
| Goal 详情 | 可点击查看权威目标；clear、切会话和 reload 后不显示旧 done/active |
| 状态一致性 | cwd、Git、model、usage、backend、lifecycle 不被旧 session 响应覆盖 |
| 目录选项 | 普通目录完整可见；隐藏目录开关、上一级、最近目录、context window 可组合使用 |
| App Server | 正文 delta、final 原位收敛、审批、用户输入、Owner restart 恢复 |
| 可靠发送 | 快速 ACK、同 ID 重试无重复、失败保留草稿、Esc 语义准确 |
| 长历史 | 快速滚动、前插锚点、resize、跨屏和移动键盘无明显回归 |
| Markdown/TeX | 表格、cases、matrix、宽公式、代码与复制 fidelity 全通过 |
| 认证安全 | 未认证拒绝、loopback/socket 权限、日志/diagnostics/CI 无敏感正文 |
| 供应链 | Actions 固定 SHA，依赖 PR 可审阅，CodeQL 与 source gate 通过 |
| Ubuntu 22.04 | Python 3.10 最低支持 lane 与真实部署 doctor 通过 |
| 普通刷新 | 新 tab/普通 reload 加载当前 revision，不要求 hard refresh |
| tmux | 名称、pane PID、宽高、换行和 desktop-owned session 不变 |

## 12. 回滚与停止条件

- 所有实现留在 feature 分支，完成门禁前不合并 `main`。
- 1.9.1 tag 指向改造前已验证提交，可作为稳定回滚点。
- 后端/前端每次迁移先共享契约测试，再删除旧实现；不使用 destructive Git 操作。
- 如果拆分导致循环依赖、bundle 重复 renderer 或性能门失败，回退该子阶段并保留更窄接口，
  不用“已经投入很多”作为继续的理由。
- 如果 CodeQL/Dependabot 因仓库权限不可启用，保留配置和本地验证证据，明确记录需要的
  GitHub 操作者动作。
- 更新和部署失败必须自动回到上一健康版本，且不得停止或重建既有 Codex/tmux 会话。

## 13. 公开与隐私规则

- 计划、提交、PR、Release notes 和 fixture 只使用匿名 session、临时目录与假凭据。
- 不提交真实 hostname、邮箱、Cloudflare 组织信息、Token、Cookie、密码、会话正文、私有
  workspace 路径或真实 tmux 名称。
- 诊断只保留版本、功能标志、计数、耗时和脱敏错误类别。
- 发布前对新增及修改文件执行 credential/private-path/body 关键词扫描，并人工复核命中。

## 14. Phase 0 基线证据

- `main`/`origin/main` 为 1.9.1 的干净提交，最近一次 Source CI 在 Python 3.10 与
  Python 3.13 均通过；公开仓库的 Latest Release 仍是 1.6.7。
- canonical source gate 通过：220 个 Owner、118 个 Gateway、60 个 CLI Python 测试，
  以及 Ruff、ESLint、Prettier、TypeScript、Node/browser tests 和可复现 bundle。
- 部署的 1.9.1 `faryo doctor` 为 24 OK、0 warning、0 failed；App Server、Owner、Gateway
  三个服务 active，只有 Owner/Gateway 在 loopback TCP 监听。
- 六个既有 tmux pane 的名称、PID 和 geometry 已生成仅供本轮前后比较的本地摘要；不把
  名称或其他私有会话信息写入仓库。
- 当前 tracked footprint 为 451 个文件、约 7.42 MB；主要热点为 Owner `server.py`
  3,747 行、Owner `app.js` 2,602 行和 Gateway `workbench.js` 1,835 行。
- 当前生成 bundle 预算实测约为 Owner 10.8 KiB gzip、Gateway 7.4 KiB gzip；后续仍以
  构建器固定的正式阈值为验收依据。
- `v1.9.1` 已精确标记改造前基线；GitHub Release workflow 通过并发布 source archive、
  两个 SHA-256 manifest 与 bootstrap installer。隔离下载复验了两个 checksum、archive
  可读性、CLI 版本和 installer 帮助入口，GitHub Latest Release 已切到 1.9.1。
- Owner 拆分后 `server.py` 为 2,940 行，比 3,747 行基线净减少约 21.5%；新建
  `session_catalog.py` 与 `session_launch.py`，旧实现已删除。
- 前端将 status 请求生命周期迁入 `status-controller.mjs`；5 项聚焦测试覆盖正常提交、会话
  漂移、取消、并发与超时，`app.js` 不再持有 status AbortController/run ID。
- 当前 canonical source gate 通过：Owner 229 项、Gateway 120 项、CLI/安装层 65 项测试，
  以及 Ruff、格式、TypeScript、Node、bundle 可复现性和浏览器静态门。

## 15. Phase 6 验收证据

- PR 1 的 Source CI 在 Ubuntu 22.04/Python 3.10 与 Ubuntu 24.04/Python 3.13 两个 lane
  均通过；CodeQL Python 与 JavaScript/TypeScript 两个 job 均通过。首次 workflow 无 job
  失败被定位为 workflow-level concurrency 误用 job-only `matrix` context，修复后 canonical
  gate 增加了对应防回归断言。
- 本机通过统一安装器建立独立 `v1.10.0` 私有 venv 并切换服务；`faryo doctor` 为 24 OK、
  0 warning、0 failed，App Server、Owner、Gateway 均 active，当前版本链接指向 v1.10.0。
- 真实认证 Gateway 在 430x820 完成 3 页各 10 条历史、直接跳页、完整目录/隐藏开关/context
  与双后台选择；实际新建 App Server 会话到 ready 后成功清理。Resume 缺失 cwd 的签名重试
  也通过。
- 390x844 普通 reload 验证 4 个 TUI 会话均显示 `Codex TUI (tmux)`，1 个 App Server 会话
  显示 `Codex App Server`；界面没有旧 wire 名称，加载的 Owner 资产 revision 为 v1.10.0。
  其中一个真实 Active Goal 可按需打开，objective 关闭后清空且未进入 local/session storage。
- 隔离真实 Codex 0.149.0 App Server 产生 16 次可观察正文长度增长，delta/final/turn complete、
  Markdown/TeX、JSONL 收敛、无正文 replay journal、真实审批、`request_user_input` 与进行中
  Owner 重连全部通过；浏览器发送 ACK 为 68 ms，普通 reload 后仍原位收敛并渲染 KaTeX。
- 匿名 40 轮/480 个公式段落历史完成初始 12、预取 24、最终 40 轮懒加载与 40 个问题导航；
  快速滚动最大 frame 17 ms、无 long task，Raw→Chat、移动布局、35 附件条、沉浸全屏与临时
  tmux geometry 均通过。
- 匿名 TUI delivery matrix 完成 20 条幂等发送、剪贴板图片、网络/后台/歧义恢复、失败草稿和
  双会话隔离；无重复提交，所有临时 tmux/Owner/proxy fixture 已清理。
- 部署前后六个真实 pane PID 与宽度相同。唯一 geometry 摘要差异可还原为一个 63 列 pane
  高度 47→25，且部署后存在 63x26 attached client；Owner 日志明确 `pane_width=0`，代码和
  日志均无 resize，因此不把客户端自主尺寸变化伪报为 Faryo 回归或强行覆盖。
- 真实部署发现一个 TUI 与持久 App Server record 同名的 `faryoN` 冲突，证明“新建却打开旧
  对话”来自双后台各自分配名称。升级后冲突 record 原子迁移到空闲编号，App Server/tmux
  交集为 0；原有 7 个 pane 的名称、PID、宽高在部署和两次真实启动验收前后完全不变。
- 430x820 认证 Gateway 对 App Server 与 TUI 各完成一次真实 New：两者都直接进入唯一的
  Options 页、选择目录、得到启动前不存在的新 session、达到正确后台 ready 状态并清理。
  多工作站匿名 fixture 在同一页切换工作站和目录，无旧 route sheet；三页各 10 条历史和
  直接跳页继续通过。
- 最终提交的 Ubuntu 22.04/Python 3.10、Ubuntu 24.04/Python 3.13 Source CI 与 Python、
  JavaScript/TypeScript CodeQL 全绿；PR 1 合并到 `main`。`v1.10.0` Release workflow 再次
  通过 canonical gate，发布且设为 Latest 的资产只有源码归档、两个 SHA-256 文件和安装
  脚本；下载复验得到 `Faryo 1.10.0`。

## 16. v1.10.3 持久 Activity 修复

- [x] 复现 Owner 重连后 `thread/read` 保留搜索和文件修改、但遗漏已完成命令的历史缺口；
  rollout 本体仍保留工具调用，因此不是数据丢失。
- [x] 增加增量 offset index，只读取完整 JSONL 记录，并按 Codex 保存的 turn/call ID 恢复
  command、search 与 edit 摘要；不投影 reasoning、命令输出或补丁正文。
- [x] durable 与 live process 先按稳定 ID、再按有界摘要 multiset 去重；App Server 的问题
  identity、历史分页和实时 item identity 保持不变。
- [x] 折叠标题直接列出 command/edit/search 数量；长命令继续二级折叠，普通 reload 获取
  当前资产版本。
- [ ] 完整 source/browser/deployment gate、真实重连验收、推送与 source-only v1.10.3 发布。
